{
  "name": "autheo-web",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"const{cpSync}=require('fs');cpSync('public','dist',{recursive:true,force:true});console.log('Assets copied')\" && node scripts/fix-css.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
